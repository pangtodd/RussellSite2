---
title: Mark Russell bibliography
image: https://stirring-beignet-ce08d6.netlify.app/images/flickCroppy.webp
description: complete bibliography of Mark Russell's published works.
layout: base.njk
--- 
<div class="hero">
    <img src="/assets/images/disappointed-cropped.jpg" class="d-block w-100 booksHero" alt="Hero Image">
</div>
<div class="container mt-3">

<section id="books">
  <div class="container mt-3">
    <h1 class="text-center">Books</h1>
    <h2 class="text-center">Comic books. And book books.</h2>
    <!-- Visible cards section -->
    {% set books = collections.books %}
    {% if books.length > 0 %}
      {% for book in books %}
        {% if loop.index0 % 4 ==0 %}
          <div class="row">
        {% endif %}
      <div class="col-lg-3 mt-3">
        <div class="card">
          <div class="image">
          {% if "https://res.cloudinary.com" in book.data.image %}
            <img 
              src="https://res.cloudinary.com/dfbfqlfkc/image/upload/q_auto,f_auto,w_400/{{ book.data.image }}"
              srcset="
                https://res.cloudinary.com/dfbfqlfkc/image/upload/q_auto,f_auto,w_400/{{ book.data.image }} 400w,
                https://res.cloudinary.com/dfbfqlfkc/image/upload/q_auto,f_auto,w_600/{{ book.data.image }} 600w,
                https://res.cloudinary.com/dfbfqlfkc/image/upload/q_auto,f_auto,w_800/{{ book.data.image }} 800w
              "
              sizes="(max-width: 767px) 100vw, 25vw"
              class="card-img-top" 
              alt="cover of {{ book.data.title }}"
              width="200"
              height="300"
            >
            {% else %}
            <img
              class="card-img-top"
              src="{{ book.data.image }}"
              alt="cover of {{ book.data.title }}"
              width="200"
              height="300"
            >
          {% endif %}
          </div>
          <div class="details">
            <div class="center">
              <h3>{{ book.data.title }}</h3>
              <p>
                {{ book.content | safe }}
                {%if book.data.quote%}
                  <br />-{{book.data.quote}} 
                {% endif %}
                <br />Art by {{book.data.artist}}
              </p>
              <a
                href="{{book.data.link}}"
                class="btn btn-success"
                >buy now</a
              >
            </div>
          </div>
        </div>
      </div>
      {% if loop.index % 4 == 0 or loop.last%}
        </div>
      {% endif %}
    {% endfor %}
    {% endif %}
</section>